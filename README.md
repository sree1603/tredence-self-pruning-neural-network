# tredence-self-pruning-neural-network
Implementation of a self-pruning neural network for CIFAR-10 using learnable sigmoid gates and L1 sparsity regularization, with experiments analyzing the accuracy–sparsity trade-off across λ values.
